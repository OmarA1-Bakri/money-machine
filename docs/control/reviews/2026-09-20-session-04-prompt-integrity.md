# Session 04 prompt-integrity review

**Prompt:** `prompts/implementation/07_SESSION_04_AGENT_RUNTIME_AND_ROSTER.md`
**Verified SHA-256:** `beb748811662b90c06098e51ab14a691f634f56900daf05c57e0fbdec0f826a3`, equal to the manifest's declared value in `prompts/implementation/MANIFEST.json` line 42. The extraction is authentic; the defects below are in the instruction set, not the extraction.
**Date:** 2026-09-20. **Procedure:** `docs/PROMPT_INTEGRITY_REVIEW.md` (D-0026).

## Verdicts

| Dimension | Verdict |
|---|---|
| Fidelity to the authorities and to Session 03 | CLEAR — one medium finding |
| Safety and executability against this repository | CLEAR — two medium findings |
| Exit-criteria gameability | NOT CLEAR — multiple criteria have cheap fakes |

## Findings

**M1 MEDIUM — Prompt ordering suggests immediate Exit 78 lift; Session 02/03 addenda supersede.** Action 9 (lines 140–149) requires runtime integration tests proving orchestrator-leases-job → agent-executes → result-persists → event-emits → successor-creates, and the exit criteria (lines 160–167) require "Agent runner integrates with durable jobs." The prompt's natural reading implies Exit 78 removal and live worker claiming once those integration tests pass. However, Session 02's addendum point 1 said "Job claiming is commissioned in Session 03," and Session 03's addendum point 4 superseded that with "commissioning evidence, agent promotion to COMMISSIONED, and removal of exit 78 are Session 04's scope." The prompt does not explicitly state when Exit 78 lifts. An executor reading only this prompt might implement full worker claiming and lift the exit at this session's close. The addendum clarifies: Exit 78 stays until a decision record + commissioning evidence gate explicitly authorize removal; session-04 implementation proves the runtime works with deterministic fake providers in integration tests but does not claim production jobs until that gate passes.

**M2 MEDIUM — Agent commissioning vocabulary is incomplete.** Action 5 (lines 72–93) requires registering all sixteen agents with commissioning state and instructs: "Implement real working versions now for A01 Shop Orchestrator, A02 Account & Integration diagnostics, shared deterministic rule agents that do not require external providers. Register later-domain agents as UNCOMMISSIONED, not fake-complete." The prompt does not define what "real working version" means in terms of commissioning state transitions or evidence requirements. The canonical `AgentCommissioningState` enum (from Session 02) is `DESIGNED`/`IMPLEMENTED`/`TESTED`/`COMMISSIONED`/`SUSPENDED`. "Real working version" could mean `IMPLEMENTED` (code exists), `TESTED` (integration tests pass), or `COMMISSIONED` (operator-approved evidence recorded). The addendum clarifies: A01 and A02 are promoted to `TESTED` with passing integration tests using fake providers; all sixteen agents are registered in configuration and database; agents without Session 04 implementation remain at `DESIGNED` with empty `commissioning_evidence`; `COMMISSIONED` requires operator decision record per Session 03 addendum point 4.

**M3 MEDIUM — Prompt store location conflicts with Action 2 and Session 01 structure.** Action 2 (lines 29–39) says "Every agent prompt must be stored in `src/money_machine/agents/prompts`," but Session 01's canonical structure (D-0019, `02_CANONICAL_REPOSITORY_STRUCTURE.md`) defines `src/money_machine/agents/` as the logical-agent implementation module with `__init__.py`, `base.py`, `registry.py`, `runner.py`, and per-agent modules like `shop_orchestrator.py`. Prompts are text assets, not Python code, and do not belong under `src/`. The addendum corrects this: agent prompts live at `prompts/agents/` (parallel to `prompts/implementation/`), versioned, hashable, and referenced by `prompt_versions` table rows; the prompt store loads them from that directory. Tests prove prompt existence, hash verification, section validation, and secret-freeness.

**M4 MEDIUM — Subagent review vocabulary is ambiguous.** Action 6 (lines 95–105) requires "bounded internal specialist reviews through the LLM provider" with constraints: "owning job remains singular," "reviews are attached as artifacts," "reviews do not mutate provider state," "final structured output comes from the owning agent," and "cost and run count are recorded." The prompt does not define "bounded" (token limit, tool restriction, or conversation depth), "specialist reviews" (a separate prompt, tool call, or multi-turn LLM invocation), or how reviews "attach as artifacts" (database column, file reference, or embedded JSON). The addendum clarifies: subagent review support means an agent implementation can invoke the LLM provider multiple times within a single agent run (e.g., first pass → self-critique → revision), each invocation recorded in `agent_runs` with distinct `run_number` under the same job; artifacts (review text, intermediate outputs) are stored as `agent_artifacts` rows with `artifact_type` and file references; "bounded" means token budget per run and maximum sub-invocations per job, both configurable in `config/agents.yaml`; no external provider mutations occur during reviews; the final `AgentResult` with typed output comes from the last invocation only.

**M5 MEDIUM — Tool permissions examples are illustrative, not exhaustive.** Action 4 (lines 57–69) lists tool-permission examples: "Research: Etsy read/browser, web capture, storage. Builder: Notion and asset tools. Publisher: Etsy mutation tools. Analytics: Etsy stats read. Support: Etsy messages, Notion repair and listing update. Orchestrator: no direct provider mutations; it creates jobs." These are high-level examples and do not map to specific tool identifiers. The addendum binds implementation to the typed tool registry: each agent's `allowed_tools` in `config/agents.yaml` lists tool identifiers from `ToolRegistry` (e.g., `etsy.read_listing`, `notion.create_page`, `browser.navigate`, `storage.write_blob`); the registry enforces tool permissions at runtime, refusing agent calls to unlisted tools; the orchestrator agent's allowlist includes only job-creation and workflow-inspection tools, no external provider mutations; tests prove tool-permission enforcement (allowed tool succeeds, disallowed tool raises error).

**Gameability.** "Provider abstraction works" (exit line 161) is satisfied by a stub LLMProvider that returns hardcoded JSON without calling any model. "Prompt registry and hashes work" (line 162) is satisfied by one prompt file with a passing hash check and no version-tracking table. "Agent runner integrates with durable jobs" (line 163) is satisfied by a single integration test that creates one job row, invokes one deterministic fake handler, and writes one result with no successor or event. "All sixteen agents are registered" (line 164) is satisfied by sixteen rows in the database with no configuration file, no prompt paths, and no tool permissions. "Unimplemented agents are honestly uncommissioned" (line 165) is satisfied by a database column set to `DESIGNED` with no contract tests proving they cannot execute production jobs. "Contract and runtime tests pass" (line 166) is satisfied by one passing test with no coverage of schema validation, timeout handling, retry on transport errors, tool permission enforcement, or commissioning state checks. All evidence keys are self-reported satisfaction claims rather than machine-earned receipts from CI or database schema enforcement.

## Corrective addendum

This is the instruction set Session 04 executes. The prompt remains the unamended source of record.

1. **Exit 78 stays until decision record + commissioning evidence gate.** Worker and scheduler continue to exit `EXIT_UNAVAILABLE = 78` per Session 03's verified closure. Implement the agent runtime (provider abstraction, prompt store, agent base/registry/runner, tool permissions, observability), prove it works with deterministic fake providers in contract and integration tests, and register all sixteen agents with commissioning state. A01 Shop Orchestrator and A02 Account & Integration diagnostics are promoted to `TESTED` (code implemented, integration tests pass with fake providers). Agents without Session 04 implementation remain at `DESIGNED` with empty `commissioning_evidence`. No agent is promoted to `COMMISSIONED` in this session. Production worker claiming (lease acquisition for real agent execution) requires: (a) a decision record explicitly authorizing Exit 78 removal with justification, timing, and rollback plan; (b) commissioning evidence proving each promoted agent's integration tests pass, prompt hash verification works, tool permissions are enforced, and structured output validation succeeds; (c) operator approval recorded in `docs/control/DECISIONS.md`. Until that gate passes, worker entrypoints exit 78, and scheduler entrypoints exit 78. Integration tests use `JobEnvelope` with deterministic fake handlers and prove the orchestrator-lease → agent-execute → result-persist → event-emit → successor-create flow without live provider calls. Session 04's scope is runtime implementation and testing; commissioning and exit-78 removal are post-implementation gates.

2. **Agent commissioning state transitions: DESIGNED → IMPLEMENTED → TESTED → COMMISSIONED.** "Real working version" (Action 5, line 89) means `TESTED`: code exists, integration tests pass with fake providers, contract tests prove schema validation and tool permission enforcement. A01 and A02 reach `TESTED` in this session. Other agents are registered at `DESIGNED` with configuration entries, database rows, empty `commissioning_evidence`, and contract tests proving they refuse production execution while uncommissioned. `COMMISSIONED` requires operator-approved evidence (decision record, integration test receipts, prompt hash verification, tool permission enforcement proof) recorded in the `agents` table's `commissioning_evidence` JSONB column. Session 04 delivers the runtime and proves A01/A02 work; operator commissioning and live execution are post-session gates.

3. **Agent prompts live at `prompts/agents/`, not under `src/`.** Action 2's "stored in `src/money_machine/agents/prompts`" (line 32) is corrected: agent prompts are text assets and live at `prompts/agents/<agent-id>/<version>.md` (e.g., `prompts/agents/shop-orchestrator/v1.md`). Each agent's `config/agents.yaml` entry includes `prompt_path` and `prompt_version`; the prompt store loads from `prompts/agents/` by agent ID and version. The `prompt_versions` table tracks agent prompt hashes, version numbers, and deployment timestamps. Tests prove: prompt file exists at the declared path, SHA-256 matches the stored hash, required sections (Purpose, Input Contract, Output Contract, Instructions, Tool Permissions) are present, and no runtime secrets (API keys, tokens, credentials) are embedded. "Adapt the playbook's prompts into agent system prompts while preserving their business logic" (line 39) means: extract relevant business rules from the playbook PDF (e.g., research task scoring criteria, portfolio decision thresholds, publication timing rules) and encode them as agent instructions in the prompt file; encode structured data (thresholds, weights, enums) in configuration YAML, not prompts. Prompts reference configuration keys rather than hardcoding values.

4. **Subagent review support: multi-invocation runs with artifact tracking.** "Bounded internal specialist reviews through the LLM provider" (Action 6, line 96) means: an agent implementation can invoke the LLM provider multiple times within a single job execution (e.g., first-pass generation → self-critique → revision), each invocation recorded in `agent_runs` with a distinct `run_number` under the same `job_id`. Each invocation's prompt, model, input hash, output, tool calls, tokens, and cost are recorded. Intermediate artifacts (critique text, reasoning traces, alternate candidates) are stored as `agent_artifacts` rows with `artifact_type` (e.g., `review`, `intermediate_output`, `reasoning`) and file references or embedded JSON. "Bounded" means: token budget per invocation (from `config/agents.yaml` agent entry), maximum sub-invocations per job (default 3), and timeout per invocation (agent-level timeout divided by max sub-invocations). The owning job remains singular (one `jobs` row, one final `AgentResult`); reviews do not spawn separate jobs or workflows. Reviews do not mutate provider state: no Etsy writes, Notion creates, or external API mutations during self-review; only `EXTERNAL_READ` operations (Etsy read, browser navigation, storage read) are permitted in review sub-invocations. The final `AgentResult` with typed structured output (e.g., `ProductSpec`, `DedupeResult`) comes from the last invocation only; intermediate outputs are artifacts. Cost and run count: sum tokens and cost across all sub-invocations for the job; emit one PostHog event per job with aggregated usage.

5. **Tool permissions: typed registry with agent allowlists.** Action 4's examples (lines 58–67) are high-level categories; implementation uses a typed `ToolRegistry` mapping tool identifiers (e.g., `etsy.read_listing`, `notion.create_page`, `browser.navigate`, `storage.write_blob`) to tool handlers with declared side-effect classes (`READ_ONLY`, `EXTERNAL_READ`, `EXTERNAL_WRITE`). Each agent's `config/agents.yaml` entry includes `allowed_tools: [tool_id, ...]`. `AgentRunner` enforces permissions: if an agent calls a tool not in its allowlist, the runner raises `ToolPermissionError` and fails the job. The Shop Orchestrator agent's allowlist includes only orchestration tools (`job.create`, `workflow.inspect`, `job.retry`, `configuration.read`), no direct external provider mutations (no `etsy.publish_listing`, `notion.create_page`, `browser.navigate`). Contract tests prove: A01 with `allowed_tools: ["job.create"]` can create a job and cannot call `etsy.read_listing`; A02 with `allowed_tools: ["etsy.read_shop_info", "configuration.read"]` can read Etsy and cannot write Notion; an agent calling a disallowed tool fails with `ToolPermissionError`. Map the prompt's example categories to concrete tool identifiers: Research → `["etsy.read_listing", "etsy.search_listings", "browser.navigate", "storage.write_blob"]`, Builder → `["notion.create_page", "notion.upload_asset", "storage.write_blob"]`, Publisher → `["etsy.publish_listing", "etsy.update_listing"]`, Analytics → `["etsy.read_shop_stats", "etsy.list_transactions"]`, Support → `["etsy.read_messages", "etsy.send_message", "notion.update_page", "etsy.update_listing"]`.

6. **Provider abstraction: OpenAI-compatible + deterministic fake + structured-output validation.** Action 1 (lines 13–25) requires `LLMProvider` interface, current OpenAI-compatible provider, deterministic fake provider, structured-output validator, timeout handling, retry for transport/schema errors, token/cost metadata, and prompt/model version recording. Implementation: `LLMProvider` abstract base with `complete(prompt: str, output_schema: Type[BaseModel], timeout: float) -> AgentResult` method; `OpenAIProvider` uses OpenAI SDK with structured outputs (JSON schema mode), timeout via SDK config, retry on `APIConnectionError`/`APITimeoutError` (exponential backoff, max 3 attempts), token/cost from response `usage` field, and prompt hash + model version in result metadata; `FakeProvider` returns deterministic outputs matching the declared schema (e.g., `ProductSpec(name="Test Product", ...)` for product-spec jobs) with no external calls, deterministic token counts, and zero cost; all providers validate output against the `output_schema` Pydantic model and raise `SchemaValidationError` if invalid. Tests prove: OpenAI provider calls the real API with correct parameters (skip if no API key), fake provider returns valid outputs without network calls, schema validation rejects malformed JSON, transport errors trigger retry, and timeout errors fail after retry budget exhausted.

7. **Prompt store: versioned, hashable, loadable, testable, secret-free.** Action 2 (lines 29–39) requires prompt versioning, hashing, loading by ID, section validation, and secret-freeness. Implementation: `prompts/agents/<agent-id>/<version>.md` files; `prompt_versions` table with `(agent_id, version, prompt_path, sha256_hash, deployed_at)` rows; `PromptStore.load(agent_id: str, version: str) -> str` reads the file, verifies SHA-256 matches the stored hash, and returns the content; section validation asserts presence of `## Purpose`, `## Input Contract`, `## Output Contract`, `## Instructions`, and `## Tool Permissions` headers; secret-freeness asserts no API key, token, password, or credential patterns (regex `api[_-]?key|token|password|secret|credential`) in the prompt text. Tests prove: load with valid agent/version succeeds, load with hash mismatch raises `PromptIntegrityError`, missing required section raises `PromptValidationError`, prompt with embedded API key raises `PromptSecretError`, and prompt with no secrets passes. Populate `prompt_versions` rows for A01 and A02 in Session 04; other agents have placeholder entries with empty `prompt_path` until their prompts are written.

8. **Agent base and registry: definition, context, base, registry, runner, commissioning state.** Action 3 (lines 42–53) requires `AgentDefinition`, `AgentContext`, `BaseAgent`, `AgentRegistry`, `ToolRegistry`, `AgentRunner`, structured result validation, run receipts, error capture, and commissioning state. Implementation: `AgentDefinition` dataclass with `(agent_id, name, purpose, input_contract, output_contract, prompt_path, prompt_version, allowed_tools, side_effect_class, timeout, max_attempts, commissioning_state, commissioning_evidence)` fields loaded from `config/agents.yaml` and `agents` table; `AgentContext` carries `(job_envelope, agent_definition, provider, tool_registry)` into agent execution; `BaseAgent` abstract with `execute(context: AgentContext) -> AgentResult` method; `AgentRegistry` loads all agent definitions from YAML + database, indexed by agent ID; `ToolRegistry` maps tool identifiers to handlers; `AgentRunner` orchestrates: load agent definition, load prompt, construct context, call provider with prompt + input + output schema, validate result, record `agent_runs` row, attach artifacts, return `AgentResult` with typed output and event. Structured result validation: parse provider output as the agent's declared `output_contract` Pydantic model; if invalid, retry once with validation errors in prompt; if still invalid, fail with `SchemaValidationError`. Error capture: catch all exceptions, record in `agent_runs.error_message`, emit `AGENT_EXECUTION_FAILED` event. Commissioning state: `AgentRunner` checks `commissioning_state` before execution; if `DESIGNED` (uncommissioned), refuse production jobs and raise `AgentNotCommissionedError`; if `TESTED` or `COMMISSIONED`, proceed. Tests prove: registry loads sixteen agents, runner executes A01 with fake provider and returns valid `ProductSpec`, runner refuses to execute uncommissioned agent on production job, schema validation failure retries then fails, timeout raises `AgentTimeoutError`.

9. **Agent-run observability: database + structured logs + PostHog events.** Action 7 (lines 108–121) requires storing job, agent, input hash, prompt version, model, start/end, status, structured output, validation errors, tool calls, artifacts, cost/usage, and emitting structured logs and PostHog-compatible events. Implementation: `agent_runs` table with columns `(id, job_id, agent_id, run_number, prompt_version, model, input_hash, start_at, end_at, status, output_json, validation_errors, token_count, cost, error_message)`, indexed by `job_id` and `agent_id`; `agent_artifacts` table with `(id, run_id, artifact_type, artifact_path, metadata_json)`; `agent_tool_calls` table with `(id, run_id, tool_id, input_hash, output_hash, duration_ms)`; structured logs: JSON lines with `{"timestamp", "level", "event_type", "job_id", "agent_id", "run_id", "message", "metadata"}` written to stdout; PostHog events: `{"distinct_id": agent_id, "event": "agent_execution_completed", "properties": {"job_id", "run_id", "status", "duration_ms", "token_count", "cost", "model", "prompt_version"}}` emitted via `posthog.capture()` with offline queueing (no live PostHog connection required in Session 04). Tests prove: completed run writes `agent_runs` row with all fields populated, artifacts attach correctly, tool calls record, logs emit as JSON, PostHog event queues without network call.

10. **Contract tests: prompt exists, schema imports, config validates, tools resolve, side-effect class declared, fake provider produces valid result, malformed output fails closed, uncommissioned agent refuses production.** Action 8 (lines 124–134) lists eight contract tests per roster entry. Implementation: sixteen parameterized test cases, one per agent in `config/agents.yaml`. Each test: (1) assert prompt file exists at `agent_definition.prompt_path`; (2) import `agent_definition.input_contract` and `output_contract` as Pydantic models without exception; (3) validate agent YAML entry has all required fields; (4) resolve each tool in `allowed_tools` from `ToolRegistry` without exception; (5) assert `side_effect_class` is one of `READ_ONLY`, `EXTERNAL_READ`, `EXTERNAL_WRITE`; (6) invoke `AgentRunner.execute()` with fake provider and assert result matches `output_contract` schema; (7) invoke with fake provider returning malformed JSON and assert `SchemaValidationError` raised; (8) attempt production job execution with `commissioning_state=DESIGNED` and assert `AgentNotCommissionedError` raised. For agents not implemented in Session 04 (all except A01/A02), test (6) skips execution (no prompt yet), test (7) skips (no fake output), test (8) proves refusal only. A01 and A02 pass all eight tests.

11. **Runtime integration tests: orchestrator leases, runner executes, result persists, event emits, successor creates, schema retry, final invalid fails, restart retains history.** Action 9 (lines 137–148) requires proving the full orchestrator-to-agent-to-successor flow. Implementation: integration test creates a three-job workflow (research → dedupe → build) with typed contracts, deterministic fake handlers for research and dedupe agents (A01 orchestrator not invoked directly; test uses typed job handlers), uses real lease acquisition (`FOR UPDATE SKIP LOCKED`), real `AgentRunner.execute()` with fake provider, real `SuccessorFactory`, and real PostgreSQL transactions. Scenarios: (1) research job completes, emits `SHORTLIST_READY`, successor dedupe job created; (2) dedupe job with fake provider returning invalid JSON on first call, valid on retry, proves schema retry; (3) dedupe job with fake provider returning invalid JSON twice, job fails with `SchemaValidationError`; (4) orchestrator leases research job, runner executes, result persists, event emits, successor created, all in one transaction; (5) kill worker after result persist but before event, restart, assert event and successor missing, prove transaction rollback; (6) complete job, record `agent_runs` row, restart runner, assert run history retained. Tests use throwaway databases per `scripts/run_affected_tests.sh` and real async execution.

12. **Register all sixteen agents in `config/agents.yaml` and database.** Action 5 (lines 72–93) requires ID, name, purpose, input/output contracts, prompt path/version, allowed tools, side-effect class, timeout, max attempts, commissioning state for all sixteen agents. Implementation: `config/agents.yaml` entries for Shop Orchestrator (A01), Account & Integration Diagnostics (A02), Research (A03), Concept (A04), Validation (A05), Teardown (A06), Notion Build (A07), Variant Build (A08), QA (A09), Listing Copy (A10), Assets (A11), Draft Listing (A12), Preflight (A13), Publisher (A14), Analytics (A15), Support (A16). A01 and A02 have `commissioning_state: TESTED`, prompt paths, input/output contracts, and passing integration tests. A03–A16 have `commissioning_state: DESIGNED`, placeholder prompt paths (empty or TBD), typed contracts from Session 01, allowed tools from Action 4 examples, and contract tests proving they refuse production execution while uncommissioned. Database seed inserts sixteen `agents` rows with IDs, names, purposes, commissioning states, and empty `commissioning_evidence` for all except A01/A02 (which have test receipts). Tests prove: registry loads sixteen agents, A01/A02 are `TESTED`, A03–A16 are `DESIGNED`, uncommissioned agents refuse production jobs.

13. **Bounded slices with integration checkpoints.** Slice 1: `LLMProvider` interface, fake provider, OpenAI provider stub, structured-output validation, timeout, and provider tests. Slice 2: Prompt store, `prompt_versions` table, hash verification, section validation, secret detection, and prompt tests. Slice 3: Agent base, definition, context, registry, tool registry, runner, commissioning state check, and contract tests for all sixteen agents. Slice 4: Agent-run observability (tables, structured logs, PostHog events) and observability tests. Slice 5: Runtime integration tests (lease → execute → persist → event → successor, schema retry, transaction rollback, restart retention) and A01/A02 commissioning to `TESTED`. Integrate and review after slices 3 and 5.

14. **Harden the exit criteria.** "Provider abstraction works" means: `LLMProvider` interface exists, OpenAI provider calls the SDK with correct parameters (integration test with live API key, skipped if key missing), fake provider returns deterministic valid outputs, structured-output validation rejects malformed JSON and triggers retry, timeout raises error after retry exhausted, transport errors retry with exponential backoff, and token/cost metadata are recorded. "Prompt registry and hashes work" means: `prompt_versions` table exists with schema including `agent_id`, `version`, `prompt_path`, `sha256_hash`, `deployed_at`; `PromptStore.load()` reads the file and verifies the hash matches; hash mismatch raises `PromptIntegrityError`; section validation and secret detection tests pass; A01 and A02 have registered prompt entries with correct hashes. "Agent runner integrates with durable jobs" means: integration test creates a three-job workflow (research → dedupe → build), uses real lease acquisition, real `AgentRunner.execute()` with fake provider, real result persistence, real event emission, real successor creation, and proves transaction rollback on exception. "All sixteen agents are registered" means: `config/agents.yaml` has sixteen entries, database has sixteen `agents` rows, `AgentRegistry` loads all sixteen, contract tests run for all sixteen. "Unimplemented agents are honestly uncommissioned" means: A03–A16 have `commissioning_state: DESIGNED`, contract tests prove they refuse production execution with `AgentNotCommissionedError`, and empty `commissioning_evidence`. "Contract and runtime tests pass" means: 128 contract tests (8 per agent × 16 agents), 11 provider/prompt/runner unit tests, 6 runtime integration tests, all green in CI. Machine-earned receipts: pytest exit code 0, database schema migration revision, `alembic check` output, constraint-rejection logs, PostHog event queue file existence.

15. **Standing safety, restated.** Simulation only, including in tests. No publication, purchase, spend, customer message, or provider mutation. External effects only through adapters with explicit modes, idempotency, and reconciliation. Never commit secrets, environment files, tokens, cookies, browser profiles, runtime state, generated evidence, or the PDF. Preserve both immutable sources byte-for-byte and preserve user changes. Tests must not mutate the shared development database or leave orphan test databases; use `scripts/run_affected_tests.sh` for database-backed suites. Agent prompts must not embed API keys, tokens, or credentials; secret detection tests enforce this.

16. **Reviews at exit.** Two independent reviews scoped to agent runtime architecture (provider abstraction, prompt store, registry, runner, commissioning state, tool permissions) and prompt contract integrity (prompt versioning, hash verification, section validation, business logic preservation, configuration separation). Each returns CLEAR or NOT CLEAR with numbered findings recorded under `docs/control/reviews/`, and every critical and high finding is remediated and re-reviewed before the closure commit.

## Deferrals

| Item | Reason | Owner |
|---|---|---|
| Live OpenAI API connection | Session 04 uses fake provider; live provider integration requires API key, billing, and rate limit handling | Session 05 or operator provisioning |
| Exit 78 removal and production worker claiming | Requires decision record + commissioning evidence gate per Session 03 contract | Post-Session-04 gate; likely Session 05 or 06 |
| Domain-specific agent implementation (A03–A16 except A01/A02) | Session 04 delivers runtime and roster; domain agents implemented in Sessions 05–11 per session scope | Sessions 05 (research), 06 (Notion), 07 (build/variant/QA), 08 (merchandising/assets), 09 (draft/publish), 10 (analytics), 11 (support) |
| PostHog live connection | Session 04 emits PostHog-compatible events with offline queueing; live PostHog integration deferred | Session 12 (operator console) or first telemetry session |
| Subagent review production usage | Session 04 implements the multi-invocation run structure and artifact tracking; production usage of self-review flows deferred until agents with review logic are implemented | Sessions 05–11 as each domain agent adds review steps |

## What the prompt gets right

Byte-faithful extraction (SHA-256 verified). The prompt correctly scopes Session 04 to runtime implementation, not commissioning or Exit 78 removal, even though the ordering implies live execution. The sixteen-agent roster matches Session 01's architecture decision (D-0019, shop orchestrator + fifteen domain agents). The provider abstraction requirements (structured output, timeout, retry, cost metadata) match production LLM integration needs. Prompt versioning, hashing, and section validation match the playbook's prompt-integrity discipline. Tool permissions with per-agent allowlists correctly enforce the orchestrator-agent boundary (orchestrator creates jobs, domain agents execute). Subagent review support (multi-invocation runs with artifact tracking) matches the playbook's "review-first, then publish" pattern. Agent-run observability (job, agent, prompt version, model, tokens, cost) provides the audit trail required for debugging and cost management. Contract tests per roster entry prove uncommissioned agents refuse production execution, enforcing fail-closed commissioning. Runtime integration tests prove the orchestrator-to-agent-to-successor flow with real transactions and lease acquisition. The required exit code and next-prompt reference are correct.

---

**Execution begins only after this addendum is accepted and every critical and high finding is resolved or explicitly deferred with a reason and an owning session.**
